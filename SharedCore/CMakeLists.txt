# This is a placeholder CMakeLists.txt for the SharedCore directory.
# This directory will contain the "Salt Bridge" between CHTL and CHTLJS.

# Example:
# add_library(SaltBridge salt_bridge.cpp)
# target_include_directories(SaltBridge PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
